# Example waypoint list, ported from holoocean_examples/config/sv_waypoints.yaml.
# Each entry is [x, y, z] in the `marker_frame` / odom frame. z is unused by
# the XY waypoint-seeking logic but kept for marker/format compatibility.
waypoints:
  - [10.0, -301.0, 0.0]
  - [20.0, -301.0, 0.0]
  - [20.0, -311.0, 0.0]
  - [10.0, -311.0, 0.0]
