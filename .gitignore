**/__pycache__/
**/*.py[cod]
*.Zone.Identifier

# colcon output (also covered by workspaces/.gitignore)
build/
install/
log/

# BiguaSim upstream clone: installed by hand into biguasim/ inside the
# container (see BIGUASIM.md at the repo root), bind-mounted from there.
# It is its own git repo -- never committed here.
/biguasim/

# rosbags
*.bag
*.db3
*.mcap
*:Zone.Identifier
*:Zone.Identifier
